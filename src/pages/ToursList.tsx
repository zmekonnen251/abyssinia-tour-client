import AdminDashboard from '../layouts/AdminDashboard/AdminDashBoard';
import ToursList from '../features/admin/components/tours/ToursList';

const List = () => {
	return (
		<AdminDashboard>
			<ToursList />
		</AdminDashboard>
	);
};

export default List;
